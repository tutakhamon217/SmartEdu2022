export const getters = {
    year(state) {
        return state.year
    },
    user(state) {
        return state.user
    },
    currentYear(state) {
        return state.currentYear
    },
    currentSemester(state) {
        return state.currentSemester
    },
    pageAllow(state){
        return state.pageAllow
    }
}